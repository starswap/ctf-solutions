from parse import parse

gen_file = []

with open("encoder.c", "r") as f:
    line_number = None
    for line in f:
          if line_number != None:  # currently processing a line
               if line.startswith(" */")
               
          try_parse = parse("/* "encoder.py":{line_number}", line)
          if (try_parse):
               while len(gen_file) < line_number
               
               